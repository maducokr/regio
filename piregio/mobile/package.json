{
  "name": "regio-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Regio Note — Google Play APK (Capacitor)",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:copy": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "capacitor": {
    "platforms": ["android"]
  }
}
